import { TopBar } from '@/components/layout/TopBar'

export const CVPage = () => {
  return (
    <div>
      <TopBar title="CV Manager" />
      <div className="flex items-center justify-center p-12 text-text-muted">
        CV Manager — coming in Phase 3
      </div>
    </div>
  )
}
